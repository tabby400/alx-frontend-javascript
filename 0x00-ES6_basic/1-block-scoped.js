export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  // empty maybe for future use
  // eslint-disable-next-line no-empty
  if (trueOrFalse) {
  }

  return [task, task2];
}
